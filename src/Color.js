/*

import React from 'react';
import AddColor from "./AddColor";

function Color({id, hex, rgb, removeColor}) {
    return (
        <>
            <li key={id}>
                <h2>{hex}</h2>
                <h2>{rgb}</h2>
                <button onClick={() => removeColor(id)}>Delete</button>
            </li>
            <AddColor/>
        </>
    );
}

export default Color;

 */